namespace MetamaskSetup.Playwright.Meta;

/// <summary>
/// Single supported MetaMask extension build for this library. UI selectors are written for this version only;
/// use the matching unpacked extension when loading via <c>--load-extension</c>.
/// </summary>
public static class Constants
{
    public const string METAMASK_VERSION = "13.2.3";
}
